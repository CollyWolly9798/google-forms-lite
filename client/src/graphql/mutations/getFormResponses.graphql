query GetFormResponses($id: ID!) {
    form(id: $id) {
        id
        title
        questions {
            id
            text
        }
        responses {
            id
            answers {
                questionId
            }
        }
    }
}
